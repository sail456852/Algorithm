//This is copied from stackoverflow.com 
//Converting an array to a vector 
//e.g.
int x[3] = {1, 2, 3};
std::vector<int> v(x, x + sizeof x / sizeof x[0]);
//Prototype:
std::vector<int> v(BeginOfArray, EndOfArray);

//standard vector initialization takes 2 parameters (array beginning , array end ) ,these two are pointers or iterators 
